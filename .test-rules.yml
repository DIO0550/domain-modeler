# テストルール設定
# 各ルールを false にすると該当チェックを無効化できます
# ファイル単位の無効化: テストファイル先頭に // @test-rules-disable [ルール名] を記述
#
# no-conditional を無効にする理由:
# セットアップで DOM / Option の不在をテスト失敗にするための if が既存テストにあり、
# 条件分岐そのものを仕様分岐として使っているわけではない。
rules:
  no-describe: true
  no-conditional: false
  file-naming: true
